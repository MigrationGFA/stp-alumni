name: Mirror to Account A

on:
  push:
    branches:
      - main
      - branch-evans
      - branch-aisha

jobs:
  mirror:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Push to Account A
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git remote add mirror https://DEV-TNK:${{ secrets.MIRROR_TOKEN }}@github.com/DEV-TNK/stp-alumni.git
          # git remote add mirror https://x-access-token:${{ secrets.MIRROR_TOKEN }}@github.com/DEV-TNK/stp-alumni.git
          git push mirror --force --all
          git push mirror --tags
